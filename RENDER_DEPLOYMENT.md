# 🚀 Развёртывание на Render.com

Это приложение для изучения английского языка использует **Google Gemini 2.0 Flash API** для генерации примеров и подсказок.

## 📋 Предварительные требования

1. **Аккаунт на Render.com** - создайте бесплатный аккаунт на [render.com](https://render.com)
2. **Google Gemini API Key** - получите ключ на [Google AI Studio](https://aistudio.google.com/apikey)
3. **Git репозиторий** - код должен быть в GitHub, GitLab или Bitbucket

## 🔑 Получение Gemini API Key

1. Перейдите на [Google AI Studio](https://aistudio.google.com/apikey)
2. Войдите в свой Google аккаунт
3. Нажмите **"Create API Key"**
4. Выберите проект или создайте новый
5. Скопируйте полученный API ключ (начинается с `AIzaSy...`)

## 📦 Развёртывание на Render.com

### Метод 1: Автоматическое развёртывание (рекомендуется)

1. **Загрузите код в Git репозиторий**
   ```bash
   git add .
   git commit -m "Prepare for Render.com deployment"
   git push origin main
   ```

2. **Создайте новый Static Site на Render.com**
   - Перейдите на [dashboard.render.com](https://dashboard.render.com)
   - Нажмите **"New +"** → **"Static Site"**
   - Подключите ваш Git репозиторий

3. **Настройте параметры развёртывания**
   - **Name**: `english-word-trainer` (или любое другое имя)
   - **Branch**: `main` (или ваша основная ветка)
   - **Build Command**: `npm install && npm run build`
   - **Publish Directory**: `dist`

4. **Добавьте переменную окружения**
   - В разделе **"Environment"** нажмите **"Add Environment Variable"**
   - **Key**: `VITE_GEMINI_API_KEY`
   - **Value**: Ваш Gemini API ключ (например: `AIzaSyB18OYv1ibsX6lNY5eqyKzrEPj0xC4cq9M`)

5. **Создайте сайт**
   - Нажмите **"Create Static Site"**
   - Render.com автоматически начнёт сборку и развёртывание

### Метод 2: Используя render.yaml

Если в вашем репозитории есть файл `render.yaml`, Render.com автоматически применит эти настройки:

```yaml
services:
  - type: web
    name: english-word-trainer
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_GEMINI_API_KEY
        sync: false
```

**Важно**: После создания сервиса не забудьте установить значение `VITE_GEMINI_API_KEY` в настройках!

## 🔧 Локальная разработка

### Установка зависимостей

```bash
npm install
```

### Настройка API ключа

1. Создайте файл `.env.local` (уже включён в .gitignore):
   ```bash
   VITE_GEMINI_API_KEY=your_api_key_here
   ```

2. Или скопируйте из примера:
   ```bash
   cp .env.example .env.local
   ```
   И замените `your_api_key_here` на ваш настоящий ключ.

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5173

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Предварительный просмотр продакшен-сборки

```bash
npm run preview
```

## 🎯 Функции приложения

- ✅ **Интерактивное обучение** - пошаговое изучение слов
- ✅ **Проверка произношения** - используется Web Speech API
- ✅ **Проверка правописания** - интерактивные упражнения
- ✅ **AI подсказки от Gemini 2.0 Flash** - умные примеры использования слов
- ✅ **80 слов для изучения** - тщательно подобранный словарь
- ✅ **Адаптивный дизайн** - работает на всех устройствах
- ✅ **Темная тема** - современный и стильный интерфейс

## 🔍 Проверка работы Gemini API

После развёртывания проверьте работу API:

1. Откройте ваше приложение
2. Начните упражнение с любым словом
3. На этапе произношения или написания нажмите **"Need a hint?"**
4. Если появляется пример предложения - API работает! 🎉
5. Если видите сообщение об ошибке - проверьте настройки API ключа

## 📊 Мониторинг использования API

1. Перейдите на [Google AI Studio](https://aistudio.google.com)
2. Откройте раздел **"API Keys"**
3. Проверьте статистику использования вашего ключа

**Бесплатный лимит Gemini API:**
- 15 запросов в минуту
- 1500 запросов в день
- 1 миллион токенов в месяц

Этого более чем достаточно для учебного приложения!

## 🛠️ Технологический стек

- **Frontend**: React 19 + TypeScript
- **Build Tool**: Vite 6
- **Styling**: Tailwind CSS (CDN)
- **AI**: Google Gemini 2.0 Flash API
- **Speech Recognition**: Web Speech API
- **Hosting**: Render.com (Static Site)

## 🔒 Безопасность

- ✅ API ключ хранится в переменных окружения
- ✅ `.env.local` добавлен в `.gitignore`
- ✅ Никогда не коммитьте API ключи в Git
- ✅ Используйте разные ключи для разработки и продакшена

## 🐛 Решение проблем

### Проблема: "API Key not configured"

**Решение**: Убедитесь, что в Render.com добавлена переменная окружения `VITE_GEMINI_API_KEY`

### Проблема: Сборка не проходит

**Решение**:
- Проверьте версию Node.js (должна быть >= 18.0.0)
- Убедитесь, что все зависимости установлены
- Проверьте логи сборки в Render.com

### Проблема: API возвращает ошибки

**Решение**:
- Проверьте правильность API ключа
- Убедитесь, что API включен в Google Cloud Console
- Проверьте лимиты использования

### Проблема: Распознавание речи не работает

**Решение**:
- Web Speech API работает только в Chrome/Edge
- Требуется HTTPS (на Render.com это есть по умолчанию)
- Разрешите доступ к микрофону в браузере

## 📝 Обновление приложения

Render.com автоматически обновляет сайт при каждом push в Git:

```bash
# Внесите изменения в код
git add .
git commit -m "Update features"
git push origin main

# Render.com автоматически пересоберёт и задеплоит приложение
```

## 🌐 Custom Domain (опционально)

1. В настройках вашего Static Site на Render.com
2. Перейдите в раздел **"Custom Domains"**
3. Нажмите **"Add Custom Domain"**
4. Следуйте инструкциям для настройки DNS

## 📄 Лицензия

Этот проект создан для образовательных целей.

## 🤝 Поддержка

Если возникли вопросы:
- Проверьте [документацию Render.com](https://render.com/docs/static-sites)
- Прочитайте [документацию Gemini API](https://ai.google.dev/docs)
- Откройте issue в репозитории проекта

---

**Приятного обучения английскому! 🎓🇬🇧**
